name: local_store
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  nested_map:
    path: ../nested_map

dev_dependencies:
  lints: any
